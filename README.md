# qsio-kernel

First executable QSO/QSIO semantic kernel.

## Demo

```bash
python -m qsio.demo
```

This creates four bounded QSOs, executes a deterministic interaction chain,
prints the QSIO ledger, verifies hashes and witnesses, replays final state,
and places every QSO into Quietus.
